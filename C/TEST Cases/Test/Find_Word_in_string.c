#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<stdlib.h>

int main()
{
    int i,j,k,len,count,positions;
    char main_string[10000],word[100], test[200];
    printf("Enter the String:");
    gets(main_string);
    printf("Enter the word to find:");
    gets(word);
    len=strlen(main_string);
    for(i=0;main_string[i]!='\0';) {
        for(j<)
    }


}