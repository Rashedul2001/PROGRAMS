// incomplete code write it to get square from floating point

#include <stdio.h>
#include <conio.h>

int main()
{
	float base, power, answer = 1;
	printf("Enter the base and power:");
	scanf("%f %f", &base, &power);
	int sepint = power;
	printf("%d ", sepint);
	// for(int i=1; i<=power; i++)
	// {
	// 	answer*=base;

	// }
	// printf("\n%.3f to the power %.3f is: %.3f", base, power, answer);
}
