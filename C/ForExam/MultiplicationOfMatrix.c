#include <stdio.h>
#include <conio.h>

int main(void)
{
    int a[10][10], b[10][10], ans[10][10], i, j, k, r1, r2, c1, c2;
    printf("Enter the number of rows and columns of first matix(Max 10) : ");
    scanf("%d %d", &r1, );
    printf("Enter the number of rows and columns of second matix(Max 10) : ");
    scanf("%d", &)
}