#include <stdio.h>
main(
 {
    int i;
    printf("Enter the number to check even or odd:");
    scanf("%d", &i);
    if (i%2==0)
     {
        printf("The number is even");

    }
    else
    printf("The number is odd");
}