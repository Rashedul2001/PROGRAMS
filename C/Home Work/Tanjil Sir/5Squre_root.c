#include<stdio.h>
#include<math.h>
int main()
{
  double i,answer;
  printf("Enter a positive number to calculate squre root:");
  scanf("%lf",&i);
  answer = sqrt(i);
  printf("\n answerr is %.3lf",answer);

}